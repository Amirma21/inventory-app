export interface Category {
    title: string
    createDate: number
    description: string
}

export interface Product {
    title: string
    quantity: number
    categoryTitle:string
    createDate:number
}